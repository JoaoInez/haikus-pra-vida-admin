import { createMuiTheme } from "@material-ui/core/styles";

export default createMuiTheme({
  palette: {
    type: "dark",
    background: {
      default: "#0a0a0a",
      paper: "#2b2d42",
    },
    primary: {
      main: "#26a8ed",
    },
    secondary: {
      main: "#fff",
    },
  },
});
